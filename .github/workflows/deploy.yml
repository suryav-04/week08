name: CD - Deploy to AKS

on:
  push:
    branches:
<<<<<<< HEAD
      - dev   # change to main if you want deployment from main
=======
      - dev
>>>>>>> da114fb (Test Azure login in CD pipeline)

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      # Checkout repo
      - name: Checkout repo
        uses: actions/checkout@v4

<<<<<<< HEAD
      # 🔑 Login with Service Principal JSON
=======
      # Azure Login
>>>>>>> da114fb (Test Azure login in CD pipeline)
      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      # Debug subscription info
      - name: Show Azure Subscription
        run: |
          az account show
          az account list -o table
